/**
 * Root compatibility shim for the package-canonical runtime reforge engine.
 */
export {
  ReforgeEngine,
} from '@agentforge/core';
export type {
  ReforgeEngineOptions,
  FeedbackAnalysis,
  RecommendedAction,
  AgentMutation,
  AgentOverride,
  ReforgePlan,
  ReforgeResult,
} from '@agentforge/core';
