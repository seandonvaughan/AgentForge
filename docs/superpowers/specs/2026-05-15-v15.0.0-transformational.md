# v15.0.0 — Transformational + Foundational

Sprint goal: turn AgentForge from a system with one working hot-path into a robust autonomous platform that engages the entire 44-agent team across foundational architecture, data subsystems, dashboard surfaces, security, and ops. This sprint deliberately spans every category in `team.yaml` (strategic, implementation, quality, utility).

**Calibration context for the scorer** (real medians from last 7 cycles):
- ci / typecheck / release-gate: ~$0.75
- fix (specific file + function target): ~$1.10
- chore (verify / document / move): ~$0.55
- feature (wire up dashboard route): ~$1.65
- migration / refactor: ~$2.00
- test (add unit / integration coverage): ~$0.90
- doc (README / CHANGELOG / spec): ~$0.55
- e2e (Playwright): ~$2.50
- security (audit + fix): ~$1.00

DO NOT estimate items at $5+ unless they span multiple subsystems with new architecture.

---

## Track 1 · Cycle Runner Foundations (architect / debugger / qa-engineer)

TODO(autonomous): Add pre-execute typecheck step in `packages/core/src/autonomous/cycle-runner.ts` between RUN and VERIFY. Run `pnpm --filter @agentforge/core build && pnpm exec tsc --noEmit --pretty false` and fail-fast if it errors. Saves $15-30 per cycle on gate rejections caused by structural code issues introduced during execute. Files: `packages/core/src/autonomous/cycle-runner.ts`. Tag: feature.

TODO(autonomous): Add a `knownDebt: string[]` field to the gate-phase prompt construction in `packages/core/src/autonomous/phase-handlers/gate-phase.ts`. Populate it by reading the last gate-verdict from `.agentforge/memory/gate-verdict.jsonl`. The gate agent must distinguish "introduced by this sprint" findings (which block merge) from "pre-existing known debt" findings (which warn but don't block). Files: `packages/core/src/autonomous/phase-handlers/gate-phase.ts`. Tag: feature.

TODO(autonomous): Add incremental cost roll-up to cycle.json. After each phase handler writes its phase JSON, update `cycle.json` with cumulative cost from all phase files written so far. Currently `cycle.cost.totalUsd` reports $0 until cycle terminal — operators see no live progress. Files: `packages/core/src/autonomous/cycle-runner.ts`, `packages/core/src/autonomous/phase-scheduler.ts`. Tag: feature.

DONE(v15.0.0): Wired `packages/core/src/predictive-planning/effort-estimator.ts` into the autonomous cycle as the second-tier fallback (LLM scorer → effort-estimator → static priority). `EffortEstimator` is instantiated inside `ScoringPipeline.effortEstimatorFallback()` (`packages/core/src/autonomous/scoring-pipeline.ts:209`) and the ladder is driven by `scoreWithFallback()` at all three call sites: `cycle-runner.ts:437`, `preview-cycle.ts:83`, `cycles-preview.ts:201`. Resilience further improved in this cycle by deriving `complexityScore` from the item's primary tag (`complexityFromTags()`) so heterogeneous backlogs produce differentiated estimates instead of collapsing to a single complexity-5 default. Covered by tests in `packages/core/src/autonomous/__tests__/scoring-pipeline-fallback.test.ts` (effort-estimator tier + `complexityFromTags` helper) and `tests/autonomous/unit/scoring-pipeline.test.ts` (third-strike ordering, budget-cap respect, p50-bypass). ✅

TODO(autonomous): Add a per-request `timeoutMs` override on `ExecutionRequest` so phase-level callers can extend the default 20-min CLI timeout for heavy reasoning tasks (scoring large backlogs, deep architectural decisions). Today every call uses the same default. Files: `packages/core/src/runtime/types.ts`, `packages/core/src/runtime/transports/claude-code-compat-transport.ts`. Tag: feature.

---

## Track 2 · Knowledge Graph Activation (dba / ml-engineer / coder)

TODO(autonomous): Add SQLite tables `knowledge_entities` (id, type, name, description, created_at, source_cycle_id, embedding BLOB) and `knowledge_relationships` (id, from_entity_id, to_entity_id, type, confidence, created_at) to the WorkspaceAdapter schema in `packages/db/src/`. Add migration. Tag: feature.

TODO(autonomous): Refactor `packages/core/src/knowledge/knowledge-graph.ts` to use the WorkspaceAdapter for persistence. Currently uses in-memory `Map<string, Entity>` that loses all data on restart. Inject `adapter: WorkspaceAdapter` via constructor, replace every `Map.set`/`Map.get` with adapter calls. Files: `packages/core/src/knowledge/knowledge-graph.ts`, `packages/server/src/routes/v5/knowledge.ts`. Tag: migration.

TODO(autonomous): Inject `writeKnowledgeEntry()` calls in `packages/core/src/autonomous/phase-handlers/audit-phase.ts` (after findings are produced) and `phase-handlers/review-phase.ts` (after review is produced). Each call should extract entity-like terms from the agent response and persist them. This is the root cause of the always-empty /knowledge page — nothing writes to the graph. Tag: feature.

TODO(autonomous): Implement `RelationshipMapper.inferFromText()` in `packages/core/src/knowledge/relationship-mapper.ts` (currently a stub). Use simple heuristics: noun-phrase extraction + co-occurrence within K tokens → suggested relationships. Trigger after each entity batch-insert. Tag: feature.

TODO(autonomous): Wire embeddings table (1 row currently) to KG entities. After each `addEntity()` write, also compute an embedding via the existing `packages/embeddings/` pipeline and store. Enables semantic search across the KG. Files: `packages/core/src/knowledge/knowledge-graph.ts`, `packages/server/src/routes/v5/embeddings.ts`. Tag: feature.

TODO(autonomous): Add `knowledge` as a searchable type in `packages/server/src/routes/v5/search.ts`. POST /api/v5/search currently skips KG entirely. After KG persistence lands, the search query should include knowledge entities (by name + description match). Tag: feature.

---

## Track 3 · Budget Calibration (data-analyst / performance-engineer)

TODO(autonomous): Add rolling `p50CostByTag` computation in `packages/core/src/autonomous/workspace-telemetry-adapters.ts`. For each cycle in the last 20 cycles, read `cycle.json` cost + the sprint plan's per-item tags, then compute median cost per tag (ci, fix, feature, etc.). Pass via `grounding.p50CostByTag` in `gatherGrounding()`. Tag: feature.

DONE(v15.0.0): Replaced the static-fallback flat `perItemUsd` in `packages/core/src/autonomous/scoring-pipeline.ts`'s `staticFallback()` with `p50CostByTag[item.tags[0] ?? ''] ?? defaultCost` (line 327). Tag-specific medians (fix ≈ $1.10, feature ≈ $1.65, doc ≈ $0.55) replace the flat $1.50 per-item default; the flat `perItemUsd` is retained as `defaultCost` for unrecognised tags and for the adapter-throws path. Covered by 8 unit tests in `tests/autonomous/unit/scoring-pipeline.test.ts` (`describe ScoringPipeline.staticFallback — p50CostByTag lookup`) and `packages/core/src/autonomous/__tests__/scoring-pipeline-fallback.test.ts` (`describe ScoringPipeline — staticFallback: p50CostByTag lookup`). ✅

TODO(autonomous): Add a `cost-prediction` integration test that runs scoring against the last 5 cycles' actual backlogs and asserts the LLM scorer's `estimatedCostUsd` matches actual within 3× (currently it's 6-1622×). Will catch future calibration regressions. Files: `packages/core/src/autonomous/__tests__/scoring-pipeline-calibration.test.ts`. Tag: test.

TODO(autonomous): Surface budget burn rate prediction on the cycle detail page. Compute `(currentSpent / itemsCompleted) * itemsRemaining` and show "projected final cost: $X". Files: `packages/dashboard/src/routes/cycles/[id]/+page.svelte`. Tag: feature.

---

## Track 4 · Team Engagement (architect / sprint-planner)

TODO(autonomous): Add `audit-phase.ts` agent rotation based on sprint type. If sprint tags include "research" or "experiment", use `vp-research` or `research-scientist` instead of `researcher`. If tags include "infra" or "observability", use `observability-engineer`. Falls back to `researcher`. Files: `packages/core/src/autonomous/phase-handlers/audit-phase.ts`. Tag: feature.

TODO(autonomous): Add `learn-phase.ts` agent rotation. Cost-heavy cycles → `cfo`; quality-heavy → `qa-manager`; default → `data-analyst`. Files: `packages/core/src/autonomous/phase-handlers/learn-phase.ts`. Tag: feature.

TODO(autonomous): Improve scorer prompt to penalize "BackendEngineer" / "FrontendEngineer" / "QAEngineer" style invented agentIds with an explicit clause: "If you find yourself wanting to use a name not in the agent roster above, RE-READ the roster — there is almost certainly a better match like `coder`, `frontend-dev`, or `backend-qa`. Inventing agent names degrades cycle quality." Files: `packages/core/src/autonomous/scoring-pipeline.ts`. Tag: fix.

TODO(autonomous): Add modelCap + RuntimeAdapter `applyCaps()` test coverage. Gate has flagged the missing tests twice. Cover: same-tier (no change), downgrade-with-effort:max injection, xhigh-coerce-to-max for non-Opus, effortCap independent of modelCap, fallback chain (opus → sonnet → haiku). Files: `packages/core/src/autonomous/__tests__/runtime-adapter-caps.test.ts`. Tag: test.

---

## Track 5 · Static Page Wire-up (frontend-dev / ui-engineer / coder)

TODO(autonomous): Persist GitBranchManager to SQLite. Today `/api/v5/branches` reads from an in-memory `GitBranchManager` singleton that resets on every restart. Add a `git_branches` table, refactor the manager to use the adapter. Files: `packages/server/src/routes/v5/merge-queue.ts`, `packages/core/src/git/git-branch-manager.ts`, `packages/db/src/`. Tag: feature.

TODO(autonomous): Persist approvals to SQLite. Today `packages/server/src/routes/v5/approvals.ts` uses `const approvalQueue = new Map()` with a comment saying "production: persist to DB". Add an `approvals` table, migrate the routes. Files: `packages/server/src/routes/v5/approvals.ts`, `packages/db/src/`. Tag: feature.

DONE: RuntimeJobSupervisor persistence — `runtime_jobs` and `runtime_events` tables are now written by the supervisor on every agent run. The `/jobs` page has durable history across restarts. The CLI autonomous command wires a `RuntimeJobSupervisor` via `WorkspaceManager` before each cycle; `RuntimeAdapter._runWithSupervisor()` closes the observability gap. Direct adapter tests live in `packages/db/src/__tests__/workspace-adapter-runtime-jobs.test.ts`.

TODO(autonomous): Fix /runner page svelte-check errors (MODEL_TIERS undefined per audit) + wire `/api/v5/run/history` to return real session data. Files: `packages/dashboard/src/routes/runner/+page.svelte`, `packages/server/src/routes/v5/run.ts`. Tag: fix.

TODO(autonomous): Fix /org dashboard route. Pre-diagnose: read `packages/server/src/routes/v5/org-graph.ts`, confirm what it returns vs what the page expects. Document the data contract in code comments. Then fix the page to render the actual returned shape. Last cycle timed out twice (20 min × 2) on this item due to lack of pre-diagnosis. Files: `packages/server/src/routes/v5/org-graph.ts`, `packages/dashboard/src/routes/org/+page.svelte`. Tag: fix.

TODO(autonomous): Add /sessions search filter. Sessions table has 22 rows but the UI has no filter — add `?search=` to `GET /api/v5/sessions` and a search input on the page. Files: `packages/server/src/routes/v5/index.ts`, `packages/dashboard/src/routes/sessions/+page.svelte`. Tag: feature.

TODO(autonomous): Identify what should populate the empty `kv_store` table (0 rows in workspace-default.db). Grep the codebase for `adapter.kv*` method calls; either delete the unused table or wire the missing writers. Files: `packages/db/src/`, find all callers. Tag: chore.

---

## Track 6 · Convergence Carry-over (coder / devops-engineer / debugger)

DONE(v18.1.0): Fixed CI typecheck ordering and the cycle-runner default typecheck commands. CI `type-check` job already had `pnpm build` before `pnpm exec tsc -b --noEmit` (fixed in v16.1.0). Core fix: `DEFAULT_CYCLE_CONFIG.testing.buildCommand` promoted from `pnpm --filter @agentforge/core build` to `pnpm build` (builds all 8 referenced packages), and `typeCheckCommand` changed to `pnpm exec tsc -b --noEmit --pretty false` (uses -b for project-reference graph). Added root `check:types` npm script. ✅

DONE(v15.0.0): Deleted legacy root `src/server/main.ts` and `src/server/server.ts` — entire `src/server/` tree is absent. All routes in `src/server/routes/` (flywheel.ts, search.ts, etc.) are confirmed present in `packages/server/src/routes/v5/` (dashboard-stubs.ts, search.ts, agents.ts, jobs.ts, cycles.ts, sprints.ts, and 30+ others). `package.json` bin/scripts already route exclusively through `packages/server/dist/main.js` — no root-server entrypoints remain. CONVERGENCE.md updated to mark root Fastify server as `✅ Deleted v10.7.0`. ✅

DONE(v15.0.0): Deleted root `src/builder/` tree (`agent-validator.ts`, `template-loader.ts`, `template-customizer.ts`, `team-composer.ts`, `team-writer.ts`, `index.ts`). All six files were thin shims (`export * from '@agentforge/core'`) that failed typecheck. The canonical implementations already existed at `packages/core/src/team/engine/builder/` and are fully intact. Deleting the stubs removes the typecheck error cluster with no loss of functionality. `pnpm --filter @agentforge/core exec tsc --noEmit` exits 0. ✅

DONE(v16.1.0): Fixed 5 previously-failing tests from Track A audit. Root cause: one test in `packages/core/src/autonomous/__tests__/cycle-runner-typecheck.test.ts` had incorrect token-count expectations for the `parseCommandArgs` regression test (`pnpm --filter "@agentforge/core" build` correctly produces 4 tokens, not 3). The other 4 referenced paths (`tests/autonomous/integration/execute-phase.test.ts`, `tests/server/phase-handlers-verification.test.ts`, `tests/integration/full-cycle.test.ts:327`) were stale spec paths — the actual test files (at `tests/autonomous/unit/execute-phase.test.ts`, `tests/autonomous/unit/phase-handlers-verification.test.ts`, `tests/autonomous/integration/full-cycle.test.ts`) were already passing. Full suite: 5576/5576 tests pass as of v16.1.0. ✅

DONE(v15.0.0): Release gate already present in `.github/workflows/release.yml` — fails on presence of `src/server/`, `src/builder/`, or `src/autonomous/` at tag time. Confirmed via audit of release.yml "Convergence gate — no deprecated root src/ directories" step. ✅

DONE(v17.x): Deduplicated `readCycleRecord` — canonical implementation in `packages/shared/src/cycle-record.ts` (with `CycleRecord` interface); thin re-export at `packages/server/src/lib/cycle-record.ts`; `dashboard-stubs.ts` imports from `../../lib/cycle-record.js`; `packages/dashboard/src/routes/flywheel/+page.server.ts` imports from `@agentforge/shared`. Single definition, zero divergence risk. ✅

---

## Track 7 · Quality + Docs (security-auditor / code-reviewer / documentation-writer)

TODO(autonomous): Security audit of `packages/server/`. Specifically: (a) review all CORS headers, especially `Access-Control-Allow-Origin: *` on streaming endpoints; (b) auth middleware bypass paths; (c) every endpoint that reads from `.agentforge/` for path-sanitization weakness; (d) any user-controlled values reaching `child_process.spawn`. Report findings as a markdown deliverable + open fix items for any CRITICAL/MAJOR. Tag: security.

TODO(autonomous): Write `docs/CONVERGENCE.md` — a mapping table of deprecated root entrypoints → canonical package replacement → removal milestone. Sections: Root CLI, Root Server, Root Builder, Root Autonomous. Enables the whole team to operate without guessing which surface is canonical. Tag: doc.

TODO(autonomous): Update `README.md` for v15.x state. Add a "Deprecated Surfaces" section listing root CLI and root server with target removal version. Remove v6-era command tables that no longer apply. Tag: doc.

TODO(autonomous): Document the Knowledge Graph architecture in `docs/architecture/knowledge-graph.md`. Cover: entity types, relationship types, embedding strategy, write path (audit + review hooks), query API. Targets future contributors. Tag: doc.

TODO(autonomous): Document the new team routing in `docs/architecture/agent-routing.md`. Cover: which phase defaults to which agent, the modelCap + effortCap caps, fallback chain, dynamic team.yaml scorer roster. Tag: doc.

---

## Notes for the scorer

- Items in Track 1, 2, 3 are foundational and should rank highest — they unblock everything else.
- Items in Track 6 carry technical debt that has blocked the last 3 cycles' CI green-state — ranking these prerequisite to Track 5 is correct.
- Each track maps to a distinct subset of the 44-agent team. Aim for breadth: assign each track's items to the named role categories above so the cycle engages 15-20 distinct agents.
- Tag every item with its primary type (ci, fix, chore, feature, migration, test, doc, security) for the cost calibration to work.
